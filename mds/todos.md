# 待辦清單

> **這份是什麼**：想做但還沒做的事
> **兩個分區**：
> - **開發路線**：有順序的工程任務，下次回來從第一個 ⏳ 開始
> - **功能 backlog**：想做的使用者功能，按重要程度分類，不一定按順序做，可穿插在開發路線之間
> **不寫什麼**：已完成的事——**完成即刪**（不打勾留著），由 [`進展紀錄.md`](進展紀錄.md) 負責記憶

---

## Claude Design 元件 gallery（✅ 主體完成 · 剩兩顆 fill-on-demand）

> **2026-06-24 補齊**：老闆要求「把所有 UI 介面都補進 gallery」，一口氣補完 P2/P3 ＋課程總覽組（13 顆），gallery 現 25 張、涵蓋全站主要畫面。剩課表對帳 UI 與隱藏的 Add Course 面板兩顆，維持「動到那塊才補」。
> **2026-06-19（歷史）決定暫停**：gallery 是輔助工具、非產品功能，曾因「先把系統功能做成熟」暫停 P2/P3；2026-06-24 老闆改口要求補齊。下面標準流程留著當之後補件索引。

> **這是什麼**：Claude Design 橋樑的本機端。每顆 UI 元件一個 preview 檔放 [`design/components/`](../design/components/)，第一行 `<!-- @dsCard group="…" -->` 讓 claude.ai/design 自動建卡片。詳細背景見 [`進展紀錄.md`](進展紀錄.md) 2026-06-18「搭起 Claude Design 橋樑」。
> **雲端專案**：claude.ai/design 的「HOBE 設計系統」，projectId `a7832be0-d223-4060-8449-f85457458db6`。

### 補一顆元件的標準流程
1. 從來源撈**真實 markup**（不要自己編結構）：動態卡片在 `js/*.js` 的 render 函式（找 `class="…"` 反引號模板），靜態畫面在 `補習班排程系統.html`。對應的 CSS class 在 `style.css`（用區塊註解找）。
2. 寫 `design/components/<name>.html`：第一行 `<!-- @dsCard group="Components" -->`（基礎類用 `Foundations`），`<head>` link `../../tokens.css` + `../../style.css`（這樣長相跟線上一致），body 放範例假資料的 markup，移除 onclick。
3. 更新 `design/components/README.md` 的「目前涵蓋」清單。
4. 推上雲端：DesignSync 工具 `list_projects`（拿 projectId）→ `finalize_plan`（writes 填新檔路徑、deletes 給 `[]`）→ `write_files`（用 localPath）。
5. ⚠️ **新增元件必做：補 `_ds_manifest.json` 的 cards 陣列**。只推 component HTML 不會自動建卡片——雲端卡片索引讀 `_ds_manifest.json`（編譯快照），新檔不在裡面就不顯示。做法：`get_file` 讀 `_ds_manifest.json` → 在 cards 加 `{"path":"design/components/<name>.html","group":"Components"}`（基礎類 `Foundations`，排在 Foundations 組之前）→ `finalize_plan` writes 填 `_ds_manifest.json` → `write_files` 用 inline `data` 推回。改完重整 gallery 頁就出現。
6. ⚠️ 若過程順手改了 `style.css`/`tokens.css`，記得也 `write_files` 推一份上雲端，否則雲端 gallery 渲染會跟本機不一致；改 `style.css` 還要把 HTML 的 `style.css?v=N` 加一。

### 已涵蓋（25 張卡，完整清單見 [`design/components/README.md`](../design/components/README.md)）
2026-06-24 老闆要求「把所有 UI 介面都補進 gallery」，一口氣補齊全部 P2/P3 ＋課程總覽組共 13 顆（gallery 12 → 25 張，已推雲端、已補 manifest）。基礎/框架、P1 高頻、P2 中頻、課程總覽後台、行政小工具全到齊。

### 待補（剩兩顆，維持 fill-on-demand）
- [ ] **課表對帳對帳 UI** — `js/students.js` 五桶差異對帳畫面（結構較大、互動多，動到那塊再補）
- [ ] **Add Course Inline Panel** — 新增課程內嵌面板（目前 UI 隱藏，見 backlog「新增課程功能完善」；重啟該功能時順手補）

---

## 開發路線

> 下次回來從第一個 ⏳ 開始。
> **北極星**（每步對齊的終點）：系統自己擁有課表（含老師／時段／教室／單價），核心操作全在系統內完成；**不再需要任何外部行事曆**（Google / iCloud 全退場）。完整說明見 [`系統地圖.md`](系統地圖.md)「北極星」段。

> **路線大轉向（2026-06-24 老闆拍板）**：**砍掉「對齊行事曆」整條**——既然最終要系統自有行事曆，就不繞 Google 那圈。新路線＝先把「新增課程／新增學生」做完整 → 所有課表頁從讀 Calendar 改讀系統 → cutover 清空舊資料、用新功能把現有課程/學生全部重新輸入。
> **新順序**：① 系統擁有課表（新增課程/學生 ＋ 全頁改讀系統）→ ② cutover 重建 → ③ 學費；點名/成績低風險、可平行穿插。
> **順序鐵則**：先把 ① 做到能用，才在 cutover 清資料當場重建——**絕不可先清**（會留系統全空的空窗）。
> **cutover 清空範圍（2026-06-24 定）**：**全清，含 studentList（100+ 學生也重建）、enrollments、價目表、courseSettings**；唯一保留＝**待補課清單（`makeupScheduled`）的紀錄**（跨期補課要留底）。⚠️ 待補課紀錄目前以學生名字字串記，學生重建後 id 全變，cutover 時這份要靠名字重新對回新 studentId。

- [ ] ⏳ **① 系統自己擁有課表（新增課程/學生 ＋ 全頁改讀系統）**　← **現首要 #1**
  - 為什麼：現在「猜類型/猜名單/解析備註」的痛，全因資料是逆讀 Google Calendar；改成在系統正向建立就消失。**這是最大一塊**（所有課表頁現都讀 Calendar、全要改），當正式大功能規劃、不隨手做。
  - **已有基礎**（2026-06-19~24 課程總覽後台，細節見進展紀錄）：週課表矩陣、課卡點開置中 modal（改單價/加退學生、練習課唯讀）、逐門課「需登記成績」開關、gallery 已補卡。
  - **下一步（依序）**：
    1. ⏳ **設計系統自有「課程」schema**（← 下一個動作；紅燈，先擬草案拍板）：course record 帶**穩定 id**、老師/班別、時段（星期＋時間，可多 slot）、教室、類型、單價、`needsGrade`/`isMath`；定義跟 student / enrollment / 待補課清單怎麼接、**練習課每堂不同名單**怎麼存。
    2. **新增課程**表單：寫進系統自有排程、**不寫回 Google Calendar**。
    3. **新增學生**表單：學生管理已有，搬來課程總覽當統一入口（建檔可選年級，見 backlog「新增課程功能完善」）。
    4. **全頁改讀系統**：今日／本週／時間軸／補課／點名 從讀 Calendar → 讀系統自有課表。
    5. 收尾：開放**改課名**（現為唯讀，等不再綁 Calendar join key 才開）；練習課名單從 Calendar 備註收進系統。
  - 🔑 **課程身分要含老師/分班、不能只靠課名**：週一 19:00 三班都叫「國二數學班」只差老師——綁課名會把三班學生混成一份名單。course 從建立就帶老師/分班當身分、平行分班各自獨立，此限制從根消失。
  - ⚠️ **時間軸 UI 雷**（真實密集資料才會踩到）：同教室同時段多堂家教（例：大教室 17:30–19:00 併 5 堂）會擠成團、名字被切。三選一：(a) 往下疊多排 /(b) 維持點得到就好 /(c) 併「N 個家教」一格點開。
  - 最終 Google Calendar 退成可選備份或不要。

- [ ] ⏳ **② Cutover 重建**（前置：① 做到能用）
  - 清空舊資料（**全清** enrollments ＋ studentList（100+ 學生）＋ 價目表 ＋ courseSettings）→ 用新功能把現有課程/學生**重新輸入一遍**。
  - **唯一保留＝待補課清單（`makeupScheduled`）紀錄**；它以名字字串記、學生重建後 id 全變 → cutover 要靠名字重對回新 studentId。
  - ⚠️ **順序鐵則：絕不可先清**（會留系統全空的空窗）。清掉後學生卡「修課」會 fallback 顯示凍結舊 `s.courses`（過期殘影），課程總覽名單直接空。

- [ ] ⏳ **③ 學費系統：計算 + 學費條 + 收據**（前置：①②）
  - 為什麼：要結算學費、給家長收據
  - 子項：
    - 學費頁（左側選單新增）：月度視角，列出本期所有學生 × 應繳金額、勾選誰已收
    - 學生 modal 加「繳費紀錄」分頁：個別查歷史
    - 學費條 PDF 產生器
    - 收據 PDF 產生器
  - 前置：
    - ~~收費公式~~（2026-06-12 已定：**每堂單價 × 堂數**）
    - ~~單價歸屬~~（2026-06-12 已定：**課程預設價 + 個人覆蓋**，已實作於 coursePrices + enrollment.price）
    - **系統擁有課表 + cutover 完成（課程/學生/價目表重建）**（見 ① ②，取代原「對齊行事曆」前置）
    - **學費條 / 收據格式**：有現成範本嗎？或要從零畫？
  - ⚠️ **堂數來源**：點名未做前，過渡期「實際堂數」從「**排定課程數 −（不補課的請假/曠課）**」推算（行事曆衍生）；點名做好後改用實際出席精修。學費第一版的堂數是推算的、非點到的
  - 預估：4~5 個工作段

- [ ] ⏳ **每日營運：點名 + 成績登記 + 高中數學作業**（低風險、可平行穿插，不擋學費）
  - 為什麼：每堂課都要點名 → 點名紀錄是學費「實際堂數」更精準的事實來源（取代上面的推算）；成績與作業是每日營運需求
  - 已定規格（2026-06-12）：
    - 課程設定加「需登記成績」開關（每門課手動設定，新課預設只點名）；今日課程分兩區：成績課在上、只點名的課在下（區內維持時間排序）
    - 點名記「到／遲到（記分鐘，仍算一堂）／曠課」（2026-06-19 從原「到/未到」改）；已請假/曠課的學生自動帶入顯示、不重複操作；臨時沒到的從點名面板跳既有請假/曠課流程
    - 成績：每堂每生可登**多筆**（標籤＋分數），涵蓋課前考、練習課重寫第二份考卷的情境
    - 高中作業：每天一張清單＝在學高中生 × 本期有修數學課；課名沒含「數學」的數學課在課程設定補標科目
  - ~~未定細節~~（2026-06-19 已定，schema 寫進 [`資料結構.md`](資料結構.md)）：作業狀態=已交/未交＋備註；試聽課不點名；**儲存=按期別分文件**（`attendance_<periodId>` 等，不塞 driveData，避 1MB 上限）；attendance 以 studentId 為鍵、只記出席（到＋`lateMin` 遲到分鐘，曠課以 Calendar 為準）、學費堂數=該期「到」場次數
  - ✅ **已做（2026-06-19，細節見進展紀錄）**：點名 slice 1（卡內嵌、到/遲到/曠、自動帶入鎖定、讀寫 `attendance_<期別>`）＋「需登記成績」開關（`courseSettings`）。⚠️ 點名上線前需在 Firebase Console 發佈放寬後的 `firestore.rules`（`sharedData/{docId}`）。
  - **下一步（接續）**：
    - 成績登記面板（每堂每生多筆 label+score，讀寫 `grades_<期別>`）；今日課程依 `courseNeedsGrade` 分「成績課在上／只點名在下」兩區
    - 高中數學作業清單（在學高中生 × 本期數學課，讀寫 `homework_<期別>`）
    - 學費「實際堂數」接 `attendance`（等 cutover 完成、enrollment 由新功能正向重建乾淨）
  - 預估：剩 2 個工作段

- [ ] ⏳ **請假／補課／統計也改讀登記簿（備註名字 → studentId）**　← **可能被 ① 吸收**（① 做完系統不再讀 Calendar 備註，名字→studentId 與同名消歧併入 ①/cutover；① 之前若卡片/請假落差變痛，可提前單獨做）
  - 為什麼：2026-06-14 課程卡顯示已切到登記簿，但「標記請假」的勾選名單、補課容量計算、學生統計（getStudentStats）仍讀 Calendar 備註 → 同一門課「卡片人數」與「請假可勾的人」可能不一致（登記簿有、備註沒有的學生：卡片看得到、請假勾不到）
  - 子項：
    - 請假勾選名單（absence.js `buildAbsPanel`）改用 `eventRoster`（登記簿名冊），不再讀 `e.students`
    - 補課容量計算（makeup.js 用 `e.students.length` 處）改用登記簿人數
    - `getStudentStats` 出缺勤統計從「名字比對備註」改為「studentId 比對」
    - `makeupScheduled.absentStudents` 從名字字串陣列 → 連 studentId（見 [`資料結構.md`](資料結構.md) 該段註記）
  - **難點**：請假仍要寫回 Calendar 標題 `【某某請假】`（Calendar 是名字制），所以「名字 → studentId」對應躲不掉，**同名問題會在這層爆**——需靠年級標注/辨識欄位消歧，動工前先想清楚對應策略
  - 預估：2~3 個工作段；不擋學費系統，但卡片/請假落差變痛時可提前插隊

---

## 功能 backlog

> 使用者功能，按重要程度分類。不一定按開發路線順序做，可隨時穿插

### 優先
> 對使用者體驗影響大

- [ ] 🔴 **學年封存：一鍵匯出學年報表（死線 2026-08-31，過了就來不及）**
  - 為什麼：系統只掃描當學年（9/1～隔年 8/31）的 Calendar 事件，9/1 一到，上一學年的補課、退半堂、曠課、學生統計全部從系統消失（Calendar 原始事件還在，但要手動翻）
  - 做什麼：把當學年「已完成補課、不補課退半堂、曠課、各學生出缺勤統計」匯出成 CSV 留底
  - 注意：跨 9/1 的學費結算（例如 8 月底請假、9 月初補課）依賴這份留底
  - **時程**：建議 8 月初動工（工程量約半天）、8/31 當天在系統按最終匯出（隔天 9/1 資料就換學年）
  - **若拖到沒做完**：9/1 前手動把學生管理頁與待補課清單各學期統計截圖／抄錄留底當緊急備案

### 一般
> 會做，但不急

- [ ] **手機版介面細修**（骨架與第一輪細修 2026-06-11 已完成並真機驗證）
  - 反應式項目：日常使用中發現哪裡不順手就回報、就修哪裡，沒有固定順序
- [ ] **生成可傳給家長與老師的訊息**：補課排定後自動產生訊息，先問家長該時段可否、確認後再通知老師
  - 為什麼：目前補課排定後還要人工通知，希望自動化
  - 關聯：HOBE_Bot 規劃階段二第 6 點，是 LINE 推播的前置功能
- [ ] **老師管理**：薪資計算、老師請假統計

### 想想看
> 之後再評估，或需要更多前置條件

- [~] **學生刪除：Calendar 備註字串殘留**（主要痛點已解）
  - 已解部分（2026-06-03）：軟刪除（status='畢業'）+ 歷屆分頁 + 掃描忽略歷屆生 → 畢業生不會死灰復燃、歷史完整保留
  - 剩餘部分：Calendar 課程備註、請假/補課事件標題裡的學生名字字串還在。直接打開 Google Calendar 仍會看到舊學生
  - 為什麼不急著做：屬於階段 3 enrollment（把學生歸屬從 Calendar 備註抽到資料層）的範圍，那時順帶解

- [ ] **離線寫入課表**（PWA 本體 2026-06-11 已完成打包；斷網目前只能看不能改）
  - 前置：先決定 Firestore 離線寫入的衝突解決策略

- [ ] **新增課程功能完善**（前置：先把新增課程面板從 UI 拉回來）
  - 為什麼：2026-05-28 因為使用頻率太低暫時隱藏。哪天決定重啟此功能時，順手完成這 3 個子項才會好用
  - 子項：
    - [ ] **教室下拉選單依選定時段篩選空教室**，並依學生人數決定可用教室（例如只有一人不顯示大教室）
    - [ ] **新生建檔時可選擇年級**
      - ⚠️ 目前若未選年級直接建檔，學生管理頁面看不到該學生，資料仍存在 Firestore 但因年級為空無法顯示
      - 更新此功能時需一併處理：（1）已存在的空年級學生（2）在學生管理頁面補上「未分年級」區塊讓這些學生可見
    - [ ] **同名舊生的判斷**：輸入的學生名稱若對應到多位既有學生（同名舊生），要能選擇是哪一位
