# 待辦清單

> **這份是什麼**：想做但還沒做的事
> **兩個分區**：
> - **開發路線**：有順序的工程任務，下次回來從第一個 ⏳ 開始
> - **功能 backlog**：想做的使用者功能，按重要程度分類，不一定按順序做，可穿插在開發路線之間
> **不寫什麼**：已完成的事——**完成即刪**（不打勾留著），由 [`進展紀錄.md`](進展紀錄.md) 負責記憶

---

## Claude Design 元件 gallery（✅ 主體完成 · 剩兩顆 fill-on-demand）

> **狀態（2026-07-07）**：全站 UI 已補齊，gallery 現 26 張（新增課程分頁全頁快照 `courses-page.html`）、分組最外層先分裝置：**電腦版・App 骨架／電腦版・課程頁／電腦版・待補課頁／電腦版・學生頁／電腦版・設定後台／手機版／共用・基礎**。**雲端已同步（2026-07-07）**，同步筆記與 manifest 本機鏡本在 `.design-sync/`。
> 剩新增課程/學生頁表單一顆，維持「動到那塊才補」。下面標準流程留作補件索引。

> **這是什麼**：Claude Design 橋樑的本機端。每顆 UI 元件一個 preview 檔放 [`design/components/`](../design/components/)，第一行 `<!-- @dsCard group="…" -->` 讓 claude.ai/design 自動建卡片。詳細背景見 [`進展紀錄.md`](進展紀錄.md) 2026-06-18「搭起 Claude Design 橋樑」。
> **雲端專案**：claude.ai/design 的「HOBE 設計系統」，projectId `a7832be0-d223-4060-8449-f85457458db6`。

### 補一顆元件的標準流程
1. 從來源撈**真實 markup**（不要自己編結構）：動態卡片在 `js/*.js` 的 render 函式（找 `class="…"` 反引號模板），靜態畫面在 `補習班排程系統.html`。對應的 CSS class 在 `style.css`（用區塊註解找）。
2. 寫 `design/components/<name>.html`：第一行 `<!-- @dsCard group="…" -->`，group 填七組之一（電腦版・App 骨架／電腦版・課程頁／電腦版・待補課頁／電腦版・學生頁／電腦版・設定後台／手機版／共用・基礎；手機寬度 preview 一律「手機版」、通用件進「共用・基礎」），`<head>` link `../../tokens.css` + `../../style.css`（這樣長相跟線上一致），body 放範例假資料的 markup，移除 onclick。
3. 更新 `design/components/README.md` 的「目前涵蓋」清單。
4. 推上雲端：DesignSync 工具 `list_projects`（拿 projectId）→ `finalize_plan`（writes 填新檔路徑、deletes 給 `[]`）→ `write_files`（用 localPath）。
5. ⚠️ **新增元件必做：補 `_ds_manifest.json` 的 cards 陣列**。只推 component HTML 不會自動建卡片——雲端卡片索引讀 `_ds_manifest.json`（編譯快照），新檔不在裡面就不顯示。做法：`get_file` 讀 `_ds_manifest.json` → 在 cards 加 `{"path":"design/components/<name>.html","group":"<跟該檔第一行同組>"}`，**插進同組的相鄰位置**（面板組別順序＝cards 陣列首次出現順序）→ `finalize_plan` writes 填 `_ds_manifest.json` → `write_files` 用 inline `data` 推回。改完重整 gallery 頁就出現。
6. ⚠️ 若過程順手改了 `style.css`/`tokens.css`，記得也 `write_files` 推一份上雲端，否則雲端 gallery 渲染會跟本機不一致；改 `style.css` 還要把 HTML 的 `style.css?v=N` 加一。

### 待補（剩一顆，維持 fill-on-demand）
- [ ] **新增課程/學生 頁表單** — 2026-07-04 上線的獨立頁（判型 chips＋自動命名＋多時段／學生表單＋同名警示），markup 在 `js/courses.js` renderCourseForm 與 `js/students.js` renderAddStudentForm；動到那塊再補
> 註：原列的「課表對帳 UI」已隨 2026-07-29 第 4 刀移除該功能而取消；舊 Add Course Inline Panel 也已連 markup 刪除。

---

## 開發路線

> **北極星**：系統自己擁有課表（含老師／時段／教室／單價），核心操作全在系統內完成，**不再需要任何外部行事曆**（Google / iCloud 全退場）。完整說明見 [`系統地圖.md`](系統地圖.md)「北極星」段。
> **路線大轉向（2026-06-24 老闆拍板）**：**砍掉「對齊行事曆」整條**——最終要系統自有行事曆，就不繞 Google。改走「先建功能 → 全頁改讀系統 → cutover 清空重建」。

### 工作順序（下次從第一項開始；細節見下方）

> **2026-07-16 順序轉向（老闆拍板）**：清空已按下 → 改成**先「全頁改讀系統」再重建**（重建完主頁直接看到成果、不用建兩次）。過渡策略＝**完全切換**（主頁不再讀 Calendar；Google 事件不刪、留作歷史備份）。

1. **① 全頁改讀系統（分四刀，「換水源不換水管」：schedule.js 展開器吐出與 Calendar 解析同形狀的課堂物件）**
   1. ✅ 地基＋第 1 刀（2026-07-16，老闆已驗收）：今日／本週／時間軸／hero 改讀系統課表
   2. ✅ 第 2 刀（2026-07-17，老闆已驗收）：標記請假／曠課改寫系統（`driveData.absences`，studentId＋名字雙存）＋待補課清單納入系統請假
   3. ✅ 成績登記面板（2026-07-17）：每堂每生多筆 label+score、讀寫 `grades_<期別>`、今日課程分「成績課上／只點名下」兩區　← **待老闆驗收**
   4. ✅ 第 3 刀（2026-07-17）：排補課／調課純系統——確認只寫紀錄不建 Calendar 事件、空檔改掃系統課表、場次長回主頁（可點名）、系統課調課開放　← **待老闆驗收**
   5. ✅ 第 4 刀全部完成（2026-07-29 (a) 搬遷、(b) 拔碼；2026-07-30 (c) 收尾＋登入改帳號密碼）。**Google 已徹底退場：系統不讀寫行事曆、不載入任何 Google API、登入改信箱＋密碼**
   - 註：cutover 清空工具（其白名單不含 absences）已於 2026-07-30 重建完成後移除，不會再被誤按
2. ✅ **② Cutover 重建（2026-07-30 完成）** — 清空（2026-07-16）＋ 老闆用「新增課程/學生」頁把**真實暑期課表建完**。同日清掉批 1 一次性程式碼（清空工具、`migrateCoursesToEnrollments`、`s.courses` 舊 fallback）。**系統自有課表從此是唯一真相**
3. **①收尾**（練習課考卷上傳/派卷/列印、練習課名單收進系統）　← **現首要**
4. **③ 學費系統**（前置 ①② 已備齊）— 學費單模板已定（2026-07-04），輸出圖片。**開工第一件事＝修 `effectivePrice()` 取價地雷**（見下方 ③）
5. **④ 老師薪資表**（吃點名/補課/試聽資料）— 模板與計算規則已定（2026-07-04）

> ↔ **可平行穿插**：每日營運（點名 → 成績 → 高中數學作業）　｜　⤵ **多半被 ① 吸收**：請假/補課/統計改讀登記簿

> **cutover 已走完（2026-07-16 清空 → 2026-07-30 重建完成）**：清空範圍＝studentList／enrollments／價目表／courseSettings／courses／teachers 全歸零，唯一保留待補課清單（`makeupScheduled`，名字制、靠名字對回新 studentId）；Google Calendar 事件留在 Google 當歷史備份、系統不再讀。歷史脈絡見 [`進展紀錄.md`](進展紀錄.md) 2026-07-14／07-16／07-30。

---

### 細節說明

- [ ] ⏳ **①收尾 系統自己擁有課表（主體 2026-07-30 已完工，剩收尾項）**　← **現首要 #1**
  - 為什麼：原本「猜類型/猜名單/解析備註」的痛，全因資料是逆讀 Google Calendar；改成在系統正向建立就消失。**主體已完成**：四刀改讀系統 ＋ cutover 重建（真實暑期課表已建進系統）。
  - **已有基礎**（細節見進展紀錄）：
    - 課程總覽後台（06-19~24）：週課表矩陣、課卡點開置中 modal、逐門課「需登記成績」開關
    - 新增課程/學生獨立頁（07-04）＋ 老師管理獨立頁（07-04）
    - 07-08 打磨：致命存檔 bug 修復（loadFromFirestore 漏讀 courses/teachers）、課程管理改名＋側欄重排、**年級兩層（國小可選一~六、批次升年級含國小鏈）**、學生/老師改**打字自動完成＋查無此人現場建檔**、練習課命名規則、老師管理顯示課名、刪除課程列出上課時間
    - 07-14 打磨：練習課科目**點選式多選**（每生一列、常用科目 toggle＋自訂）
    - ✅ 2026-07-16~29 四刀做完：主頁（今日／本週／日檢視／時間軸）、請假曠課、補課調課、待補課清單**全部改讀系統**，Google 行事曆已不再被讀寫
  - **schema 已整包拍板（2026-07-04）**：「旋鈕不是箱子」原則（自由欄位、自動判型、自動命名可手改）；正式欄位表在 [`資料結構.md`](資料結構.md)「系統自有課表」章（courses / teachers / studentList・enrollments 增欄 / papers 考卷 / sessions 逐堂 / salary 薪資帳 / tuition 學費帳）；設計對齊過程見進展紀錄 2026-07-04。
  - **剩下的收尾項（下一步）**：
    1. **練習課名單收進系統** ＋ 每週考卷（Drive 上傳／派卷／列印）——目前唯一還沒進系統的一塊
    2. ~~開放改課名~~（已完成：courseId 當 join key，系統課可在編輯表單改名，還有課名分段 `namePhases`）
  - 🔑 **課程身分要含老師/分班、不能只靠課名**：週一 19:00 三班都叫「國二數學班」只差老師——綁課名會把三班學生混成一份名單。course 從建立就帶老師/分班當身分、平行分班各自獨立，此限制從根消失。
  - ✅ **時間軸 UI 雷已解（2026-07-30）**：同教室同時段多堂家教（例：大教室 17:30–19:00 併 5 堂）以前會擠成團、名字被切；桌面日曆改成 Apple cascade 疊法（每層縮排、後者疊上、露出可點的左緣）＋「依教室分欄」後不再發生。**主頁的教室時間軸（renderTL）還是舊畫法**，密集時仍會擠——要不要一起換再看。
  - ✅ Google Calendar 已退成純歷史備份：系統不再讀寫它（2026-07-29 第 4 刀 (b)）。

- [ ] ⏳ **③ 學費系統：計算 + 學費條 + 收據**（前置 ①② 已備齊）
  - 為什麼：要結算學費、給家長收據
  - 🔪 **開工第一件事＝「錢的那一刀」**（跟學費一起做，牽扯到錢所以單獨切）：修下面的 `effectivePrice()` 取價地雷，順便清掉行事曆課時代的舊資料欄位 **價目表（`coursePrices`）＋ `courseSettings`**（cutover 後已無資料，但程式還在讀）
  - 子項：
    - 學費頁（左側選單新增）：月度視角，列出本期所有學生 × 應繳金額、勾選誰已收
    - 學生 modal 加「繳費紀錄」分頁：個別查歷史
    - **學費單產生器（模板 2026-07-04 已定，做到跟模板一模一樣）**：欄位＝學生姓名/班別/上課堂數/單堂費用/課程費用(堂數×單堂)/溢繳補繳(手動)/本月學費/備註；每格可手動改；**一鍵輸出圖片**（傳 LINE 給家長）
    - 收據產生器（老闆預告之後做，格式待給）
  - 前置：
    - ~~收費公式~~（2026-06-12 已定：**每堂單價 × 堂數**）
    - ~~單價歸屬~~（2026-06-12 已定：**課程預設價 + 個人覆蓋**，已實作於 coursePrices + enrollment.price）
    - ~~系統擁有課表 + cutover 完成~~（✅ 2026-07-30 完成：真實暑期課表已建進系統）
    - ⚠️ **做學費前必處理：`effectivePrice()` 只認舊價目表、不認系統課本體價**（2026-07-22 盤點發現）：`enrollment.js:102` 的 `effectivePrice(en)=en.price ?? getCourseDefaultPrice(courseTitle)` 只查 `coursePrices`，**系統課（courseId）的預設價其實在 `courses.defaultPrice`**。目前只被行事曆課 modal 用到（安全），但學費計算若直接拿它算，**每門系統課會被算成 null／0（靜默漏算，不報錯）**。修法：把 `effectivePrice` 改成 courseId-aware（有 courseId 讀 `findCourseById().defaultPrice`、否則 fallback 價目表），讓全系統只留一個正確取價入口。顯示層已在 2026-07-22 逐處修對（學生卡片／編輯面板／加課盒／課程 modal 都已分流），就差這個結算函式本身。
    - ~~學費條格式~~（2026-07-04 已定：老闆給了現用模板截圖，照做；收據格式待給）
  - ⚠️ **堂數來源**：點名未做前，過渡期「實際堂數」從「**排定課程數 −（不補課的請假/曠課）**」推算（行事曆衍生）；點名做好後改用實際出席精修。學費第一版的堂數是推算的、非點到的
  - ✅ **堂數裁切的前置已備妥（2026-07-27/29）**：期中插班/退出用 enrollment 的 `startDate`/`endDate`（含當日），判斷點＝`enrollmentActiveOn(en, 日期)`；課堂展開器已依日期給正確名冊。學費算某生某課的堂數時，**直接濾 `enrollmentActiveOn`** 就對，不要再自己算日期
  - 預估：4~5 個工作段

- [ ] ⏳ **④ 老師薪資表**（前置 ①② 已備齊；模板與規則 2026-07-04 已定，細節見進展紀錄）
  - 每月每師一張，照老闆模板做到一模一樣：月＋教師姓名 → 依班別分區（團班記到班人數、家教記時數）→ 補課區 → 試聽區 → 各區薪資總計 → 總薪資
  - **自動計薪**：每門課設「老師費率」（課程表單/課卡設定加輸入欄）；課堂開始自動長一筆：團班＝費率×到班人數、家教＝費率×時數、補課/試聽＝固定一筆
  - 每格可手動改；「日誌繳交」日期純手動填；**一鍵輸出圖片**（傳 LINE 給老師確認）
  - 資料來源＝①的收成：到班人數＝點名紀錄、補課＝待補課清單、試聽＝試聽課
  - **練習課輔導老師不在此表**：仍走打卡制（補習班既有打卡程式）

- [ ] ⏳ **每日營運：點名 + 成績登記 + 高中數學作業**（低風險、可平行穿插，不擋學費）
  - 為什麼：每堂課都要點名 → 點名紀錄是學費「實際堂數」更精準的事實來源（取代上面的推算）；成績與作業是每日營運需求
  - 已定規格（2026-06-12）：
    - 課程設定加「需登記成績」開關（每門課手動設定，新課預設只點名）；今日課程分兩區：成績課在上、只點名的課在下（區內維持時間排序）
    - 點名記「到／遲到（記分鐘，仍算一堂）／曠課」（2026-06-19 從原「到/未到」改）；已請假/曠課的學生自動帶入顯示、不重複操作；臨時沒到的從點名面板跳既有請假/曠課流程
    - 成績：每堂每生可登**多筆**（標籤＋分數），涵蓋課前考、練習課重寫第二份考卷的情境
    - 高中作業：每天一張清單＝在學高中生 × 本期有修數學課；課名沒含「數學」的數學課在課程設定補標科目
  - ~~未定細節~~（2026-06-19 已定，schema 寫進 [`資料結構.md`](資料結構.md)）：作業狀態=已交/未交＋備註；試聽課不點名；**儲存=按期別分文件**（`attendance_<periodId>` 等，不塞 driveData，避 1MB 上限）；attendance 以 studentId 為鍵、只記出席（到＋`lateMin` 遲到分鐘，曠課以 Calendar 為準）、學費堂數=該期「到」場次數
  - ✅ **已做（2026-06-19，細節見進展紀錄）**：點名 slice 1（卡內嵌、到/遲到/曠、自動帶入鎖定、讀寫 `attendance_<期別>`）＋「需登記成績」開關（`courseSettings`）。⚠️ 點名上線前需在 Firebase Console 發佈放寬後的 `firestore.rules`（`sharedData/{docId}`）。
  - **下一步（接續）**（成績登記面板 2026-07-17 已完成，升級進開發路線 ①）：
    - 高中數學作業清單（在學高中生 × 本期數學課，讀寫 `homework_<期別>`）
    - 學費「實際堂數」接 `attendance`（cutover 已完成、enrollment 已由新表單正向重建乾淨，可以做了）
  - 預估：剩 2 個工作段

- [ ] ⏳ **統計與待補課清單改用 studentId 比對（同名學生會算錯）**（① 已吸收大部分，剩這一段）
  - **① 已解掉的部分**：請假勾選名單、補課容量都改讀系統名冊了；Calendar 名字制的難點隨 Google 退場消失；系統請假紀錄已 **studentId＋name 雙存**
  - **剩下的**：`getStudentStats`（[js/students.js:157](../js/students.js#L157)）仍用**名字**比對 `absentStudents` / `students`，`makeupScheduled.absentStudents` 也還是名字字串陣列
  - 為什麼要修：兩個同名學生的請假／補課統計會**互相污染**（誰請假算到誰頭上）。錯得安靜、不報錯
  - 何時做：**學費（③）動工前**——學費要用出缺勤推堂數，這裡算錯就是錢算錯。資料面雙存已備妥，改的是比對那幾行
  - 預估：1 個工作段

---

## 功能 backlog

> 使用者功能，按重要程度分類。不一定按開發路線順序做，可隨時穿插

### 優先
> 對使用者體驗影響大

- [ ] **角色權限分級（老闆 2026-07-17 提出）**：老闆/部分行政＝薪資與總收入、老師＝當日課表、家長＝自家小孩的課程調度
  - ✅ **前置已完成（2026-07-30）**：自建帳號密碼登入已上線（Firebase Auth email/password），Google Calendar token 已退場、登入只剩身分用途
  - ⚠️ 真正的門在 firestore.rules：現在全部資料同一份 `sharedData/main` 白名單全讀寫，角色化前要**把資料按讀者拆**（家長只能讀自家學生）＝資料結構工程；畫面上藏起來不等於安全
  - 建議時機（老闆未拍板）：③學費 ④薪資做完後（老闆/行政視角才有東西可看）；分兩階段——先內部三角色（老闆/行政/老師），家長隔離最後
  - 收尾小事：全員設好密碼後刪掉登入頁的 Google 備援鈕（`.login-alt` ＋ `signInGoogle`），並到 Firebase Console 停用 Google 登入方式

### 一般
> 會做，但不急

- [ ] **補課/調課改「旋鈕式」自訂排課**（老闆 2026-07-08 要求，仿新增課程表單）
  - 現在補課走 slot picker（挑系統算出的空堂）；老闆要能**自訂**：補多長、哪幾天、在北投或石牌哪間教室——像新增課程那樣自由填時段
  - 屬補課子系統（makeup.js / absence.js / slot picker），中大工程；動工前先想清楚跟現有 slot picker 併存還是取代
- [ ] **同一堂課的請假學生可分開補課**（老闆 2026-07-08 要求）
  - 現況：一堂課的 `absentStudents` 綁成**單一筆補課**（makeup.js `scheduleMakeup`）→ 三人同缺只能排同一時段
  - 要改成：每位（或每子集）可各自排不同時段/教室；`makeupScheduled` 需從「每事件一筆」拆成「每事件×學生子集多筆」，牽動待補清單顯示、容量計算、完成判定
  - 與上一項「旋鈕式排課」相關，建議一起做

- [ ] **手機版介面細修**（骨架與第一輪細修 2026-06-11 已完成並真機驗證）
  - 反應式項目：日常使用中發現哪裡不順手就回報、就修哪裡，沒有固定順序
- [ ] **期別交界空清單提示**：每年 3/1、7/1、9/1 期別自動切換，待補課清單看起來像被清空（2026-07-04 老闆實際被嚇到一次）。改進：清單空時顯示「上一期還有 N 筆未完成」提示連結、點了切過去（使用者可見改動，做之前照紅燈流程確認）
- [ ] **生成可傳給家長與老師的訊息**：補課排定後自動產生訊息，先問家長該時段可否、確認後再通知老師
  - 為什麼：目前補課排定後還要人工通知，希望自動化
  - 關聯：HOBE_Bot 規劃階段二第 6 點，是 LINE 推播的前置功能
- [ ] **老師管理**：老師請假統計（薪資已升級為開發路線 ④ 老師薪資表；老師檔 CRUD 2026-07-04 已完成於獨立「老師管理」頁）
  - 註（2026-07-04 更新）：**授課薪資在系統算**（見 ④）；打卡制只剩**練習課輔導老師**（補習班既有打卡程式），怎麼串「要再想個辦法」

- [ ] **桌面日曆再強化**（2026-07-30 已做完 Week／Month 檢視、依教室分欄、點空白新增課程、亮/深色切換、cascade 疊法，見進展紀錄）：剩餘可選——
  - [ ] 拖曳課塊改時間／改教室（現在只能點開詳情，改時間要進編輯表單）
  - [ ] Month 檢視每格塞得下更多筆時自動增高（現在固定最多 3 筆＋「還有 N 堂」）
  - [ ] 週檢視也支援「依教室」（現在只有日檢視有；七天 × 六間教室要另想版面）

### 想想看
> 之後再評估，或需要更多前置條件

- [ ] **學年封存：一鍵匯出學年報表**（2026-07-14 老闆定調：**等系統成熟、真正配合日常營運時才需要**，原 8/31 死線撤銷——系統尚未進營運，現在的資料沒有結算價值）
  - 做什麼：把當學年「已完成補課、退半堂、曠課、各學生出缺勤統計」匯出 CSV 留底，供跨 9/1 學費結算
  - 何時重啟：系統接手日常營運後的第一個學年交界（9/1）前

- [ ] **離線寫入課表**（PWA 本體 2026-06-11 已完成打包；斷網目前只能看不能改）
  - 前置：先決定 Firestore 離線寫入的衝突解決策略

- [ ] **新增課程/學生表單細節需求**（表單本體 2026-07-04 已上線於「新增課程/學生」頁；剩餘強化項）
  - [ ] **教室下拉依選定時段篩空教室**，並依學生人數決定可用教室（例：只有一人不顯示大教室）
  - [ ] ~~空年級舊資料清理~~ → cutover 全清後自然消失（新表單年級必選，重建的學生不會再有空年級）；cutover 若久拖再撿回
